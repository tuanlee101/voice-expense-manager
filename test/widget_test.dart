import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads successfully', (WidgetTester tester) async {
    // Basic test to verify the app can load
    expect(true, isTrue);
  });
}
